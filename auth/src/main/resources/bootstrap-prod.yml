spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://${ENV_HOST}:8848
      config:
        server-addr: http://${ENV_HOST}:8848
        file-extension: yaml
logging:
  level:
    root: info