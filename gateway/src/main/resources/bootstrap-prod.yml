spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://${ENV_HOST}:8848
      config:
        server-addr: http://${ENV_HOST}:8848
        file-extension: yaml
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: 'http://circle-gateway:8201/circle-auth/rsa/publicKey' #配置RSA的公钥访问地址
  redis:
    database: 0
    port: 6379
    host: ${ENV_HOST}
    password: